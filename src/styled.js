import styled from "styled-components";

export const ScApp = styled.main`
  height: 100%;
  overflow: hidden;
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
`;

export const ScAppContainer = styled.div`
  margin: 0px auto;
  position: relative;
`;